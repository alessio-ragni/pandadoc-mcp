import { OpenAPIV3 } from 'openapi-types';
import { CliOptions } from '../types/index.js';
/**
 * Generates the TypeScript code for the MCP server
 *
 * @param api OpenAPI document
 * @param options CLI options
 * @param serverName Server name
 * @param serverVersion Server version
 * @returns Generated TypeScript code
 */
export declare function generateMcpServerCode(api: OpenAPIV3.Document, options: CliOptions, serverName: string, serverVersion: string): string;
